# next-todo-2026

스터디에서 구현해보는 nextjs 뜯어보면서 구현하기

1. 서버컴포넌트, 클라이언트 컴포넌트 선택 사용
2. 서버컴포넌트 존재로 데이터 패칭을 서버에서 처리해서 클라이언트로 보내는 JS 코드 양을 줄여서 성능 향상
3. 서버액션 개념이 있긴하지만, 실제 현업에선 사용 비추했었음
   [서버액션]

- 함수 호출처럼 직접 실행, API 계층 건너뜀
- 단 한번의 네트워크 왕복만으로 서버 코드 실행, 캐시 데이터 유효성 재검증(Revalidation), UI 업데이트 모두 처리 가능
  [API 라우트 핸들러]
- 일반적으로 클라이언트에서 API 호출, 응답 받은 후, 필요한 경우 UI를 수동으로 업데이트 하거나 별도 데이터 페칭 다시 수행
  Q) 여기서 굳이 서버 컴포넌트가 아닌 서버 액션이 필요한 이유?
- 서버 컴포넌트의 DB 호출 => 데이터 read
- 서버 액션은 수정/삭제(Write)
  ** but!! **
- 서버 액션 과다 사용은 CPU 점유율로 인해 화면이 느려짐
- 대량의 데이터 처리는 API Route가 훨씬 쾌적함
- 단순 조회 = 서버 컴포넌트 fetch / API Route에서 가져오기
- 단순 수정 = 서버 액션
- 무거운 수정 - API Route (feat. SWR, TanStack Query)
